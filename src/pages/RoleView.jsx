// RoleView — opens when a user clicks a role card on the dashboard. URL: /app/role/:roleId
// Will show the kanban board of all job applications under that role.

export default function RoleView() {
  return <h1>RoleView</h1>;
}
