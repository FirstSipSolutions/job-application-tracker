// JobDetailPage — opens when a user clicks a job card inside the kanban. URL: /app/role/:roleId/job/:jobId
// Will show one job application's full details: company, contact, notes, status history.

export default function JobDetailPage() {
  return <h1>JobDetailPage</h1>;
}
